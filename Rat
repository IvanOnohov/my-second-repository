massiv();
private massiv()
{
  for (int i=0; i <myArray.GetLength(0); i++0){
    for (int j=0; j <myArray.GetLength(1); j++){
        Console.Write(myArray[i,j] + "\t");
    }
  }Console.WriteLine();
}Console.WriteLine();